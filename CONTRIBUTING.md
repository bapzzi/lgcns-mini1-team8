# 같이 일하는 규칙

## 브랜치

`[첫 회의에서 정한다]` — 예: `main`에 직접 올리지 않고 `기능이름` 브랜치에서 작업 후 PR

## 커밋 메시지

```
[첫 회의에서 정한다]
```

## PR을 올릴 때

본문에 이 세 줄을 넣습니다.

```
Sub-task: <Notion Sub-task 주소>
확인한 것: <어떤 상태에서 무엇을 하면 이렇게 된다>
AI 사용: 예 / 아니오 — 썼다면 무엇을 시켰는지 한 줄
```

**Sub-task 주소를 붙이는 이유** — 할 일은 Notion에만 있습니다. 이 줄이 없으면 코드와 할 일이 끊깁니다.

## 리뷰

- 리뷰하는 사람 `[첫 회의에서 정한다]`
- 리뷰까지 기다리는 시간 `[첫 회의에서 정한다]`시간. 넘으면 `[첫 회의에서 정한다]`

## 규격을 바꿔야 할 때

`docs/api.md` · `docs/data.md`는 혼자 고치지 않습니다.

1. Notion 결정 표에 새 행(구분 `변경`) — 무엇을 왜 바꾸는지
2. 영향받는 파트 담당과 10분 회의
3. 합의된 뒤에 파일을 고치고, 영향받는 산출물 상태를 `검토`로 되돌립니다

## 올리면 안 되는 것

- `.env` — 값이 든 파일. `.gitignore`가 막고 있지만 강제로 올리지 않습니다
- API 키 · 비밀번호 · 토큰을 코드에 직접 쓰지 않습니다
- 이 저장소는 **공개**입니다. 올린 것은 누구나 봅니다
