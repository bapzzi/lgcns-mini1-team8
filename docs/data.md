# 데이터 구조

> 무엇을 저장하고 무엇을 저장하지 않는지. 바꾸려면 `CONTRIBUTING.md`의 "규격을 바꿔야 할 때"를 따릅니다.

## 쓰는 것

| 항목 | 값 |
|---|---|
| 데이터베이스 | `[정한다]` |
| 이름 짓기 규칙 | `[정한다]` — 표 이름·칸 이름을 어떤 형태로 |

## 표

### `[표 이름]`

**무엇을 담나** `[한 줄]`

| 칸 | 형 | 필수 | 설명 |
|---|---|---|---|
| | | | |

**안 담는 것** `[여기 안 넣기로 한 것과 이유]`

---

표마다 위 묶음을 복사해서 씁니다.

## 저장하지 않는 것

- 비밀번호를 그대로 저장하지 않습니다
- 개인을 알아볼 수 있는 값은 `[첫 회의에서 정한다]`

## 예시 데이터

발표와 테스트에 쓸 값을 어디서 가져오는지 적습니다. 지어낸 숫자를 발표에 쓰지 않습니다.

`[정한다]`
